# High-level abstractions for working with Redis/Valkey

Базовый набор абстракций для работы с Redis/Valkey, где последний выступает в роли брокера сообщений (Redis Stream).
В качестве клиентской библиотеки используется (Rueidis)[https://github.com/redis/rueidis]